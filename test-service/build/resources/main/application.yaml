spring:
  application:
    name: test-service
  security:
    oauth2:
      resourceserver:
        jwt:
          # Получение публичного ключа от Auth Service
          issuer-uri: http://localhost:8081

server:
  port: 8082
debug: true
